<input class="{{ class }}" type="{{ type }}" aria-label="{{ aria-label }}" name="{{ name }}" value="{{ value }}" id="{{ id }}" placeholder="{{ placeholder }}" {{#if required}}required{{/if}} {{#if disabled}}disabled{{/if}}>
